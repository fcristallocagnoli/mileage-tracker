export interface TicketCreation {
    ticketDate: string;
    ticketValue: number;
}

export interface TicketDTO {
    ticketId: string;
    ticketUserId: string;
    ticketUserName: string;
    ticketDate: string;
    ticketValue: number;
}
